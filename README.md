# CorTec Brain Interchange (BIC) Module

This module is used to interact with the `brain interchange` of *CorTec*.

The module controls both, the `data input io` and the `stimulation output io` interface.



